---
name: matrix-view-patterns
description: Not applicable to cursor-server (no frontend SPA). Use when building a future spec editor UI — otherwise ignore.
disable-model-invocation: true
---

# matrix-view-patterns — not used in cursor-server

This skill was ported from a React SPA project. **cursor-server** is a TypeScript/Hono API with no `web/` frontend.

Do not load for current tasks. For UI work on a future spec editor, use [mobile-first-design](../mobile-first-design/SKILL.md) or [taste-skill](../taste-skill/SKILL.md) when explicitly building marketing/editor surfaces.

Engineering guardrails: [senior-developer](../senior-developer/SKILL.md).
