interface:
  display_name: "Cursor Server"
  short_description: "Repo-specific patterns and workflows for cursor-server"
  default_prompt: "Use the cursor-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true